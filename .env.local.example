# When this is set, /api/divine calls OpenAI live.
# When unset, the route falls back to a pre-written scripted oracle.
OPENAI_API_KEY=

# Optional: override the model. Defaults to gpt-4o.
# OPENAI_MODEL=gpt-4o
