# Neon Oracle — 功能规格说明书

> 本文档**只描述功能与行为**，不包含视觉决策（配色 / 字体 / 间距 / 效果 / 牌面美术风格）。
> 视觉部分由 Claude Design 独立产出，本文档为其输入之一。

---

## 1. 产品定位

一个网页端的「AI 塔罗占卜」单页应用。用户（可选）输入一个问题，应用抽取三张塔罗牌（过去 / 现在 / 未来），由 AI 一次性生成对应三段神谕叙事。结果可保存为图片或分享链接。

**不是**聊天产品：用户与 AI 不存在多轮对话，整个体验是「输入 → 抽牌 → 阅读神谕 → 分享」的线性单次仪式。

---

## 2. 用户流程（State Machine）

```
 ┌──────────┐  start    ┌──────────┐  finish    ┌──────────┐  reveal    ┌──────────┐
 │  INTAKE  │ ────────▶ │ DRAWING  │ ─────────▶ │ REVEALED │ ─────────▶ │  ORACLE  │
 └──────────┘           └──────────┘            └──────────┘            └──────────┘
      ▲                                                                       │
      └────────────────────── reset (再占一次) ────────────────────────────────┘
```

| 状态 | 触发条件 | 用户能做什么 |
|------|----------|--------------|
| `INTAKE` | 初始 / 重置 | 选择输入或跳过问题；点击「开始占卜」 |
| `DRAWING` | 用户点击「开始」 | 观看抽牌动画（不可交互） |
| `REVEALED` | 抽牌动画结束 | 三张牌已翻开，但叙事未开始；等待 AI 流式输出 |
| `ORACLE` | AI 流式输出开始 | 阅读叙事（逐字打出）；流式完成后可保存 / 分享 / 重新占卜 |

---

## 3. 屏幕与元素清单

### 3.1 Screen A — INTAKE（入口）

**必含元素：**
- 应用名 / Logo 位
- 一句副标（产品描述，例：「问一个问题，让赛博神谕为你抽牌」）
- 问题输入框（可选输入，placeholder 给示例问题）
  - 最大长度：**200 字符**
  - 支持中文
  - 留空时按钮文案变为「随机占卜」
- 主行动按钮（CTA），文案随输入状态切换：
  - 有输入：「开始占卜」
  - 无输入：「随机占卜」
- （可选）底部细节信息：一句版权 / 项目说明

**交互：**
- 点击 CTA → 切换到 `DRAWING` 状态
- 回车键 = 触发 CTA

### 3.2 Screen B — DRAWING（抽牌中）

**必含元素：**
- 三张牌的容器位（横向排列：过去 / 现在 / 未来）
- 每个容器有位置标签（"过去" / "现在" / "未来"）
- 牌堆视觉锚点（牌从牌堆飞向三个位置）
- 一行状态提示文案（例："正在召唤神谕…"）

**交互：**
- 全程不可交互
- 时长上限：**3 秒**（避免拖沓）
- 动画结束自动进入 `REVEALED`

### 3.3 Screen C — REVEALED + ORACLE（合并显示，分阶段渲染）

**必含元素：**
- 三张牌（已翻开，正面朝上），保持 过去 / 现在 / 未来 排列
- 每张牌下方：
  - 牌名（中文 + 英文，例："愚者 · The Fool"）
  - 正位 / 逆位标识
  - 位置标签
- 用户的问题（若有输入则显示，作为上下文锚定）
- 三段神谕叙事区域，每段对应一张牌，按顺序流式出现：
  1. 第一段（对应"过去"）流完才开始第二段
  2. 第二段流完才开始第三段
  3. 三段都流完后显示完整结果与底部操作区
- 流式完成后的操作区：
  - 「保存图片」按钮（生成一张可下载的卡片图，包含三张牌 + 三段叙事 + 应用名）
  - 「复制分享链接」按钮（生成可分享 URL，他人打开能看到同一次占卜结果）
  - 「再占一次」按钮（重置到 `INTAKE`）

**交互：**
- 流式输出过程中：用户不能触发"再占一次"或分享（避免内容不完整）
- 流式完成后：上述三个按钮全部启用
- 「保存图片」点击 → 浏览器下载 `.png`
- 「复制分享链接」点击 → 写入剪贴板 + Toast 提示「已复制」

---

## 4. 数据模型

### 4.1 塔罗牌（Tarot Card）
仅使用**大阿尔卡那 22 张**（0 愚者 ~ 21 世界），不使用小阿尔卡那。

```ts
type TarotCard = {
  id: number;           // 0-21
  nameZh: string;       // 中文牌名，如 "愚者"
  nameEn: string;       // 英文牌名，如 "The Fool"
  uprightMeaning: string;   // 正位核心含义（喂给 AI 用，非展示）
  reversedMeaning: string;  // 逆位核心含义（喂给 AI 用，非展示）
  imageSlug: string;    // 对应图片资源名
};
```

### 4.2 一次占卜（Reading）

```ts
type Reading = {
  id: string;           // nanoid，作为分享链接的 key
  question: string | null;  // 用户问题（可为空）
  draws: [DrawnCard, DrawnCard, DrawnCard];  // 顺序：过去 / 现在 / 未来
  narrative: {
    past: string;
    present: string;
    future: string;
  };
  createdAt: string;    // ISO timestamp
};

type DrawnCard = {
  card: TarotCard;
  reversed: boolean;    // 是否逆位
  position: "past" | "present" | "future";
};
```

---

## 5. 抽牌逻辑

- 从 22 张中**无放回**随机抽 3 张
- 每张牌**独立** 50% 概率为逆位
- 抽牌动作在前端完成（无需后端 RNG）
- 抽到的结果连同问题一起发给后端用于生成叙事

---

## 6. AI 叙事生成（核心契约）

### 6.1 接口
- **路径**：`POST /api/divine`
- **响应**：`text/event-stream`（SSE 流式）

### 6.2 请求体
```json
{
  "question": "string | null",
  "draws": [
    { "cardId": 0, "reversed": false, "position": "past" },
    { "cardId": 14, "reversed": true, "position": "present" },
    { "cardId": 21, "reversed": false, "position": "future" }
  ]
}
```

### 6.3 响应（流式）
按顺序分段产出：
1. `past` 段落
2. `present` 段落
3. `future` 段落

每段以分隔标记区分（具体协议实现时定，例如 `<|past|>`...`<|end|>`），前端依据标记把流式文本分配到对应卡片下方。

### 6.4 AI 输出内容约束（提示词层面）
- 语气：**灵性占卜式**——具体、有画面、用预言断言而非鸡汤
- 长度：每段 **80–120 字**（中文）
- 必须呼应牌面（含正逆位）和用户问题（若有）
- 三段之间要有叙事连贯（过去 → 现在 → 未来）
- **禁止**：免责声明（"仅供娱乐"）、模型自指（"作为 AI…"）、空泛排比、emoji
- **禁止**：用户问题为空时，不要假设具体问题，使用"今日运势"作为隐含主题

### 6.5 模型与调用
- 模型：`claude-sonnet-4-6`
- 启用 streaming
- 启用 prompt caching（system prompt + 牌库描述部分静态，全局复用）

---

## 7. 分享机制

### 7.1 分享链接
- 用户点击「复制分享链接」时，前端把当前 `Reading` POST 到 `/api/reading`，得到 `id`
- 分享 URL 形如 `/r/{id}`
- 他人打开 `/r/{id}` 直接进入 `ORACLE` 状态展示该次结果（不再触发 AI 生成）

### 7.2 持久化
- MVP 阶段使用 **Vercel KV / Upstash Redis**（或 Neon Postgres），TTL 30 天
- 存储字段 = `Reading` 完整对象
- **不**收集用户身份 / IP / 任何可识别信息

### 7.3 保存图片
- 在前端用 canvas / html-to-image 把 `ORACLE` 状态的核心区域渲染为 PNG
- 文件名：`neon-oracle-{shortId}.png`

---

## 8. 错误与边界状态

| 场景 | 行为 |
|------|------|
| 用户输入超过 200 字 | 输入框限制 + 计数提示 |
| 用户输入纯空格 | 视为未输入 |
| 用户输入疑似攻击性 prompt 注入 | 不做内容审查，由 system prompt 守住人设 |
| AI 流式调用失败 / 超时（>30s） | 显示错误态："神谕暂时无法降临，请再试一次" + 重试按钮（回到 `REVEALED` 状态重新触发） |
| AI 返回内容不完整（少于三段） | 显示已收到的内容 + 一行提示"神谕未完整传达" |
| 分享链接 ID 不存在 / 已过期 | `/r/{id}` 页面显示"这份神谕已散入数据洪流" + 链接到首页 |
| 网络断开 | Toast 提示"连接已断" |

---

## 9. 性能与体验目标

- 首屏 LCP < 2.0s
- 抽牌动画 ≤ 3s（不含 AI 等待）
- AI 首字节延迟（TTFB of stream）目标 < 2s
- 移动端可用（响应式：单列布局、三张牌纵向排列）

---

## 10. 明确**不在**本规格范围内的事

> 以下交给 Claude Design 决定，不要在功能侧预设：

- 配色 / 字体 / 字号 / 字重
- 视觉效果（glow / glitch / scanline / glassmorphism 等）
- 牌面美术风格（写实 / 简约 / 像素 / etc.）
- 牌面具体图像（22 张牌的视觉表现）
- 卡片、按钮、输入框的具体形态
- 动画的视觉风格（仅功能上要求"抽牌"和"翻面"行为，不规定如何呈现）
- 排版细节（间距 / 圆角 / 阴影）
- 暗色 / 亮色模式
- Logo

---

## 11. 给视觉设计师的关键输入摘要

如果只能告诉设计师 3 件事：

1. **这是一台"占卜机"，不是聊天界面** —— 主舞台是三张牌 + 叙事，不是对话气泡。
2. **关键时刻是"翻牌瞬间"和"神谕逐字浮现"** —— 这两个时刻决定 wow，需要为它们做戏剧化的视觉/动效设计。
3. **整个流程是仪式感的线性体验**，不是工具型 dashboard。每一个状态都是一个"幕"。
